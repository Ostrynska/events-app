import Container from '../Container/Container';

const Header = () => {
  return (
    <header>
      <Container>
        <h2>Events App</h2>
      </Container>
    </header>
  );
};

export default Header;
